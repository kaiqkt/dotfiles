#!/usr/bin/env python3
"""Set the wallpaper and matching theme without changing display modes."""

import os
from pathlib import Path
import sys

generator = Path(__file__).resolve().with_name("generate-wallpaper-theme")
os.execv(sys.executable, [sys.executable, str(generator), *sys.argv[1:], "--apply", "--set-wallpaper"])
