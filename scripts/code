#!/usr/bin/env bash

set -euo pipefail

VSCODE_BIN="/Applications/Visual Studio Code.app/Contents/Resources/app/bin/code"

if [[ ! -x "${VSCODE_BIN}" ]]; then
  echo "Visual Studio Code not found at ${VSCODE_BIN}" >&2
  exit 1
fi

exec "${VSCODE_BIN}" "$@"
